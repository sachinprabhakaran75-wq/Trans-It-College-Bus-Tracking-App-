{
  "name": "trans-it-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^1.10.6"
  },
  "dependencies": {
    "@opentelemetry/api": "^1.9.0",
    "firebase-admin": "^13.6.0"
  }
}
